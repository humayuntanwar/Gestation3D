<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.humayunt.templateui">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logod"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Signinup.SigninActivity"
            android:windowSoftInputMode="adjustNothing">

        </activity>
        <activity
            android:name=".Signinup.SignupActivity"
            android:windowSoftInputMode="adjustNothing" />
        <activity
            android:name=".UserProfile"
            android:label="Gestation 3D"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Signinup.forget_Password" />
        <activity android:name=".MainPanel.twodguideview" />
        <activity android:name=".MainPanel.threedmodel"
            android:screenOrientation="landscape"/>
        <activity android:name=".MainPanel.arguide" />
        <activity android:name=".MainPanel.diets" />
        <activity android:name=".MainPanel.excercises" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".LocateHospital.hospital_MapsActivity"
            android:label="@string/title_activity_hospital__maps" />
        <activity android:name=".SingleItem.singleItemTwoDGuide" />
        <activity
            android:name=".MainPanel.LocateDoctorMap"
            android:label="@string/title_activity_locate_doctor_map" />
        <activity android:name=".SingleItem.singleItemDiet"
            android:theme="@style/AppTheme"/>
        <activity
            android:name=".SingleItem.singleItemExercise"
            android:hardwareAccelerated="false" />
        <activity
            android:name=".MainPanel.guidetry"
            android:label="Gestation Period Guide"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".HelpGuide.About" />
        <activity android:name=".HelpGuide.Features" />
        <activity android:name=".SingleItem.singleItemFeature" />
        <activity android:name=".HelpGuide.Contactus" />
        <activity android:name=".HelpGuide.TermsCondition" />
        <activity android:name=".HelpGuide.Faq" />
        <activity android:name=".HelpGuide.WatchVideo" />
        <activity android:name=".SidePanel.QuizActivity" />
        <activity android:name=".SidePanel.Result" />
        <activity android:name=".Signinup.SelectSignUp" />
        <activity android:name=".Signinup.SignUpDoctor"
            android:windowSoftInputMode="adjustNothing"/>
        <activity
            android:name=".MainPanel.DoctorList"
            android:label="Avaliable Doctors"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Signinup.DoctorSignIn"
            android:windowSoftInputMode="adjustNothing"/>
        <activity android:name=".Signinup.SelectSignIn">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>

</manifest>